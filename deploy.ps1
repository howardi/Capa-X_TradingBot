# Deploy to Google Cloud Run (Windows PowerShell)
# Wrapper for the Python deployment script which handles .env variables safely

Write-Host "🚀 Starting Deployment Wrapper..."
python scripts/deploy_with_env.py
